# Telegram Bot Service

Микросервис для запуска и управления множеством Telegram ботов с поддержкой workflow и AI.

## Возможности

- 🤖 **Множество ботов** - один сервис обрабатывает ботов для всех бизнесов (мультитенантность)
- 🔄 **Workflow Engine** - выполнение настраиваемых сценариев
- 🧠 **AI интеграция** - OpenAI, Anthropic с поддержкой RAG
- 📊 **Логирование** - все сообщения и executions в БД
- ⚡ **Очереди** - Asynq + Redis для асинхронных задач

## Технологии

- **Go 1.22** - основной язык
- **sqlc** - type-safe SQL без ORM
- **pgx/v5** - PostgreSQL драйвер
- **telebot** - Telegram Bot API
- **go-openai** - OpenAI клиент
- **asynq** - Redis очереди
- **pgvector** - векторный поиск для RAG

## Установка

```bash
# 1. Установите зависимости
go mod download

# 2. Установите sqlc (для генерации кода из SQL)
go install github.com/sqlc-dev/sqlc/cmd/sqlc@latest

# 3. Сгенерируйте код из SQL
sqlc generate

# 4. Настройте .env
cp .env.example .env
# Отредактируйте .env файл

# 5. Создайте файл с паролем Redis (для Docker secrets)
mkdir -p secrets
echo "9;S%oH!-dVvPZ:" > secrets/redis_password.txt

# 6. Запустите
go run cmd/bot/main.go
```

## Настройка Redis

Сервис использует внешний Redis на `109.73.193.175:6379`.

**Для локальной разработки:**
- Пароль указывается в `.env` файле через `REDIS_PASSWORD`

**Для production (Docker):**
- Пароль загружается из `/run/secrets/redis_password` (Docker secrets)
- Создайте файл `secrets/redis_password.txt` перед запуском docker-compose

```bash
# Подключение к Redis вручную:
redis-cli -h 109.73.193.175 -p 6379 --user default --pass '9;S%oH!-dVvPZ:'
```

## Архитектура

### Как работает множество ботов:

1. При старте сервис загружает все `telegram_bots` где `is_active = true`
2. Для каждого бота создается `BotInstance` в отдельной горутине
3. Каждый бот обрабатывает сообщения независимо
4. Данные изолированы через `profile_id` (RLS в PostgreSQL)

### Масштабирование:

- Можно запустить несколько инстансов сервиса
- Redis для координации (lock на profile_id)
- Каждый инстанс обрабатывает свою часть ботов

### Workflow Execution:

1. **Триггер** → команда, сообщение, webhook, расписание
2. **Загрузка** → nodes и edges из БД
3. **Выполнение** → последовательно по связям
4. **Контекст** → переменные передаются между узлами
5. **Результат** → сохраняется в `telegram_executions`

## Следующие шаги

- [ ] Реализовать все типы узлов (delay, condition, loop)
- [ ] RAG поиск через pgvector
- [ ] Asynq для delay и schedule триггеров
- [ ] Anthropic клиент
- [ ] Webhook сервер для входящих запросов
- [ ] Метрики и мониторинг

## Запуск в production

```bash
# Docker
docker build -t telegram-bot-service .
docker run -d --env-file .env telegram-bot-service

# Или через docker-compose
docker-compose up -d
```
